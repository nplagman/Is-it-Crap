<style>
hr { 
    /*display: block;*/
    border-color: #8f8f8f;
    border-width: 0px;
    max-width: 80%;
    margin-bottom: 0px;
    margin-top: 0px;
} 
</style>


    <div class="thumbnail" style="min-width: 200px; border: 0px; box-shadow: none; margin-bottom: 10px; padding: 2px;">
      <%= link_to image_tag(product.image.large.to_s), product if product.image %>
      <!--pretty sure the 'if' doesn't do anything-->
      <div class="caption">
      <hr>
        <h4 id="boldproduct" class="responsive-width" style="margin-left: 5px;"><%= product.name %></h4>
        <p id="briefshow"><%= product.price %></p><!-- 
        <% if product.verdict.present? %>
        <p class="verdict"><%= link_to (product.verdict), product, class: "btn btn-danger responsive-width" %></p>
        <% else %>
        <p class="verdict"><%= link_to (product.goodverdict), product, class: "btn btn-success responsive-width" %></p>
            <% end %> -->
      </div>
    </div>





