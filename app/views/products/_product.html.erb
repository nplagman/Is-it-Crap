<%= link_to image_tag(product.image.large_thumb.to_s), product if product.image %>
<div class="center">
<h3><%= product.name %></h3>



            <p><%= product.brief %></p>
            
            <h2 class="verdict"><%= product.verdict %></h2>

</div>
