<%= link_to image_tag(product.image.large_thumb.to_s), product if product.image %>

<div class="center" >
            <p id="size16"><%= product.name %></p>

            <p id="briefshow"><%= product.brief %></p>

            <p class="verdict"><%= link_to (product.verdict), product, class: "btn btn-danger" %></p>
</div>





