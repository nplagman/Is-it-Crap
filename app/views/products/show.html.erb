<div class="back">
  <%= link_to 'All Products', products_path, class: "btn btn-primary" %>

  <% if can? :update, @product %>
<!-- only admin can see button to edit -->
  <%= link_to 'Edit Product', edit_product_path(@product), class: "btn btn-primary" %>
  <%= link_to 'New Product', new_product_path(@product), class: "btn btn-primary" %>

<!--   Look at line 2, it's link to products_path, ie where all the products are. To link back to the relevant one, it's singular, product_path. Then I just threw the "new" or "edit" in front, and put @product in the parenths, I assume so it knows what product.  -->




  <% end %>
</div>


<div class="container">
  <div class="row">
    <div class="col-md-6">

    <%= image_tag(@product.main_picture, style: 'max-width: 100%') %>
    
</div>
    <div class="col-md-6">
      <h2><%= @product.name %></h2>      
        
      <p class="openred" style="font-size: 24px; color: #FF0000;"><%= @product.price %></p>
                
          <% if @product.part_number.present? %>
          Part #:
          <%= @product.part_number %>
          <% end %>
              
<br><br>
    <ul>
      <% if @product.bullet1.present? %>
        <li><%= @product.bullet1 %></li>
      <% end %>

      <% if @product.bullet2.present? %>
        <li><%= @product.bullet2 %></li>
      <% end %>

      <% if @product.bullet3.present? %>
        <li><%= @product.bullet3 %></li>
      <% end %>

      <% if @product.bullet4.present? %>
        <li><%= @product.bullet4 %></li>
      <% end %>
    </ul>
      

    </div>

        
  </div>




      <div class="row">
<!-- Each image uploaded in the images entry will show -->

<% @product.images.each do |img| %>

<%= image_tag img, style: "max-width: 500px;" %>
<!-- only admin can see the X for image removal -->
<% if can? :update, @product %>

<%= link_to "x", remove_attachment_product_path(img), method: :delete %>

<% end %>

<% end %>
      </div>

    
</div>


